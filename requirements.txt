# ============================================================
# RNAseek - Python pip requirements
# ============================================================
# NOTE: This file lists only DIRECT pip dependencies.
# Conda packages (R, bioinformatics CLI tools) are managed
# via environment.yml. See README.md for full setup.
# ============================================================

# ── Web Framework & Infrastructure ──
django>=5.2,<6.0
djangorestframework>=3.16,<4.0
celery[redis]>=5.6,<6.0
redis>=6.0,<7.0
django-cors-headers>=4.9,<5.0
gunicorn>=25.0,<26.0
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.1,<2.0
channels>=4.0,<5.0
channels-redis>=4.2,<5.0
daphne>=4.0,<5.0
whitenoise>=6.6,<7.0
django-celery-beat>=2.7,<3.0

# ── Scientific Core ──
numpy>=2.0
pandas>=2.3
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.0
h5py>=3.9
statsmodels>=0.14

# ── Python-R Bridge (requires R headers — install via conda env) ──
rpy2>=3.6,<4.0

# ── Single-Cell / Spatial (install via conda env for GPU/R support) ──
anndata>=0.10
mudata>=0.3
scanpy>=1.9
# squidpy>=1.4
# scvi-tools>=1.1
# tangram-sc>=1.0

# ── Bioinformatics Analysis ──
gseapy>=1.1
lifelines>=0.29
# mofapy2>=0.7
arboreto>=0.1.6
# pywgcna — install via: pip install 'setuptools<70' && pip install pywgcna && pip install 'setuptools>=75'
multiqc>=1.25

# ── Utilities (pulled by above, pinned for reproducibility) ──
# torch>=2.0  # install via conda for CUDA support
seaborn>=0.13
tqdm>=4.60
requests>=2.28
